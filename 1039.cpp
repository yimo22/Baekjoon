#include <iostream>
#include <string>
#include <queue>

using namespace std;


int bfs(string data, int depth){
  queue<string> q;
  char * temp ;
  for( int breathe= 0;breathe<data.length();breathe++){
    for(int i=0;i<data.length();i++){
      for(int j=i+1;j<data.length();j++){
        if(data[j] == '0' && i == 0)
        char * ptr = (char *) malloc(sizeof(char) * data.length());
        data.copy(ptr,data.length());
        char ch_i = data[i];
        char ch_j = data[j];
        
          continue;
        
      }
    }
    q.push()
  }
  

}

int main(){
  string s;
  cin >> s;

}